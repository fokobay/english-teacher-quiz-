groq==0.13.0
httpx==0.27.2
Pillow==10.4.0
numpy==1.26.4
requests==2.32.3
gTTS==2.5.1
moviepy==1.0.3
imageio==2.34.2
imageio-ffmpeg==0.5.1
decorator==4.4.2
# BUG FIX: proglog is a required dependency of moviepy but not pinned,
# causing random ImportError on clean installs.
proglog>=0.1.10
# BUG FIX: tqdm is pulled by groq SDK internals; without it some
# environments raise ImportError at runtime.
tqdm>=4.0.0
